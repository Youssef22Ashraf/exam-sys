{
  "name": "exam-system",
  "version": "1.0.0",
  "private": true,
  "description": "Workplace Assessment and Remote Examination System",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push && node dist/index.js",
    "seed": "cd backend && npm run seed"
  },
  "keywords": [
    "exam",
    "proctoring",
    "assessment"
  ],
  "author": "",
  "license": "ISC"
}
